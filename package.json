{
  "name": "commitlint-and-husky",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "postinstall": "sh -c \"npx --no-install husky install && npx --no-install husky add .husky/commit-msg 'npx --no-install commitlint --edit $1'\"",
    "prepublishOnly": "pinst --disable",
    "postpublish": "pinst --enable"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@commitlint/cli": "17.0.1",
    "@commitlint/config-conventional": "17.0.0",
    "husky": "8.0.1",
    "pinst": "3.0.0"
  }
}
